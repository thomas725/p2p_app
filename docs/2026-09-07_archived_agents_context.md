# Archived content removed from AGENTS.md on 2026-09-07

AGENTS.md was reduced to instruction-type content only; everything below is the
historical record that was removed (goal, done-index, decision rationale, backlog
recap, and descriptive context). The live, forward-looking rules live in AGENTS.md
("Key Decisions" there is now the compressed actionable subset).

## Goal (original — completed)

- Fix broken TUI click handlers (peer-click bug + missing message-click) and port the Flutter `PeerInfoScreen` into the TUI as a new PeerInfo tab.
- Adapt the TUI peer list to mirror Flutter's sortable peer table (columns Name, DM count, Broadcast count, Last seen; each sortable by key or header click).
- Make the TUI Peers tab scrollable with PageUp/PageDown (plus Home/End), show a total peer count, and use ratatui's stateful `Table` widgets so the viewport scrolls visually while keeping the selected row visible.
- Fix Peers-tab sluggishness with many peers (per-frame SQLite nickname lookups per row) by materializing only the visible window of rows, and remove the "Peers: N" bar between the tab bar and tab content.
- Make switching the Peers-tab sort column fast (it did one fresh-SQLite display-name lookup per peer on every full-list sort) by memoizing `get_peer_display_name`, warming the cache lazily the first time the Peers tab is opened, and making the sort reorder O(n).

## Done index (as it pointed from AGENTS.md)

Completed work is archived as dated docs (see `docs/`, oldest → newest):
- `docs/2026-08-29_tui_peerinfo_tab_and_peer_table.md` — PeerInfo tab port, message/peer click fixes, sortable peer-table adaptation, strict clippy + Settings tab (commits `38241ed`, `33ab735`, `ea741d0`, `92c9b07`, `640305e`/`e5fb1ac`).
- `docs/2026-09-01_tui_polish_and_peer_perf.md` — ConnectedTracker (connected-only Settings), Peers paging + stateful Table, "Peers: N" bar removal, windowed rows, display-name memoization + O(n) sort, ID-suffix tail, Ctrl+I(I kitty)/Ctrl+P binding saga, bug fixes/DRY, shared history constants, unread banner, libp2p-quic removal, test-only removal, parallel-test DB race fix (commits `79981fc`…`dcb90f4`).
- `docs/2026-09-02_mobile_frb_flutter_parity.md` — FRB codegen panic fix + 2.13.0, dependency upgrades, FRB/Dart peer-sort helpers, bulk peers-with-stats, bulk aggregation, First-seen column both frontends, Broadcast-column parity, backlog items 2/3/5/8 verification + drift guard + widget tests, dead-FRB cleanup (commits `eff4232`, `66e7b53`, `f8c61ab`, `d94ae6d`, `8fca899`, `20add35`, `0199280`…`8a7809d`).
- `docs/2026-09-03_broadcast_recipients_and_dioxus_removal.md` — `broadcast_recipients` table rework, migration version-collision fix, Dioxus desktop frontend removal (commits `c44ef35`, `c5ce9cd`).

## Key Decisions (full text — rationale archived)

- Ratatui's stateful `Table` + `TableState` is the official "widget that allows scrolling visually" — it scrolls to keep the selected row visible. `peer_table_visible_range` reimplements its windowing as a pure function so (a) unit tests run without a widget, (b) the click handler and renderer agree on the visible window, and (c) `AppState.peer_table_offset` serves as the anchor fed into windowing each render. Verified against the ratatui 0.3.2 source: `render_stateful_widget` mutates `TableState.offset` in `render_rows` (`state.offset = start_index`) and reads it in `visible_rows`.
- **The originally "failing" render test was a bogus assertion, not a scroll bug**: `test_render_peers_scrolls_viewport_but_keeps_selection_visible` failed because display-name cells are **truncated to the name column width** in the buffer (`organic-rockhopper (pee…`), so `text.contains("peer-29")`/`contains("peer-00")` can never hold. The scroll actually worked (verified by mapping petnames→row indexes: rendered rows 10..29). Rewrote the test to give each peer a distinct `last_seen` second (`15:26:{i:02}`) and assert `contains("15:26:29")`, `!contains("15:26:00")`, exactly 20 visible rows, and the `Connected Peers (30)` title.
- **Diesel `table!` unit structs are handled by module-ignore, not an FRB upgrade**: the panic is a long-standing FRB limitation (identical reports from FRB 2.1.0–2.3.0; the maintainer's standing reply is "feel free to PR"), so upgrading FRB alone would not fix codegen. The working fix marks the whole `generated` module (now hosting every diesel table in the crate, incl. the relocated `peer_name_history`) with `/// flutter_rust_bridge:ignore`; both 2.12.0 and 2.13.0 then generate cleanly. The user still chose to bump **2.12.0 → 2.13.0**.
- PeerInfo is a **transient dynamic tab** (like DM), not a fixed tab: inserted after DMs, before Log/Settings. Dedup by peer ID.
- Click row→message mapping reuses library helpers; `calc_visible_strings` returns `(visible, start)` where `start` is the first visible message index (for auto-scroll it equals `total - visible`), used to map the relative row back to the absolute message index.
- `terminal_width` stored on `AppState` (set in render loop via `usize::from(f.area().width)`) so click mapping matches wrapping; default 0 treated as width 1 (only before first render).
- **PeerInfo binding is tab-specific**: plain `i` opens PeerInfo only on the **Peers** tab (where there is no text input), so `i` stays typeable in the Chat/DM message boxes. On **Direct/DM** tabs the binding is **Ctrl+I** on kitty terminals (WezTerm, `AppState.kitty_keyboard_active` true from crossterm's `supports_keyboard_enhancement()`) — arriving as `Char('i')`+CONTROL — and **Ctrl+P** (`Char('p')`+CONTROL, from `0x10` DLE) on **every** terminal: on non-kitty terminals it is the only Peer Info binding (both Ctrl+I and Ctrl+Shift+I collapse to the bare `0x09` Tab byte there), and on kitty terminals it is a bonus fallback alongside Ctrl+I. Bare Tab always cycles tabs on every tab and terminal, in both modes. `Tab`+CONTROL (an alternate CSI-u spelling of Ctrl+I a conforming terminal should never send in practice) is a no-op everywhere including Direct tabs. The bottom shortcut bar only documents the binding where it is active (kitty Direct tabs show both `Ctrl+I/Ctrl+P`).
- **Display-name memoization keeps the DB-backed resolution path**: the cache stores the exact string `get_peer_display_name` would compute (local → received → generated petname, with the short-ID suffix), so the sort and the rendered table always agree — including the lazy petname **write** for silent peers that only exists on the DB path (swapping the sort to the in-memory nickname maps would have made names inconsistent with the render). Staleness is prevented by invalidating exactly where nicknames change: the `impl_set_peer_field!` macro (all peer-nickname setters) and `peers::save_peer` (may assign a petname to a brand-new peer).
- **Warm the cache lazily on first Peers-tab open, not at startup** (user-chosen): the one-time N-lookup cost is deferred to the first frame that draws the Peers content (`AppState.peer_names_warmed` set inside the render loop's draw closure). Zero cost if the user never opens the Peers tab; later-discovered peers warm on demand (one cache miss each). Warm-at-startup (Flutter-style eager `_refreshPeers`) was offered but rejected to avoid a boot-time tax on large peer lists.
- **The sort must never drop peers**: feeding `peers.as_slices().0` into `sort_peers_table` loses every peer in the wrapped (second) ring segment — `peers.as_slices().0` is the *head..cap* range (`[1,2,3]` for a wrap where element 8 sits at buf[0], e.g. fill-8/pop-1/push-1). `sort_peers_by_column` therefore iterates the whole `VecDeque` and rebuilds from a `HashMap<&str, PeerRecord>`.
- **One canonical last-seen parser**: `parse_last_seen_ms` lives in `src/fmt.rs` (space-or-T `%Y-%m-%dT%H:%M:%S`, 0 on failure) and is the single source for both the TUI sort comparator (via `crate::fmt::`) and the FRB/mobile wrapper — Dart therefore never needs its own `DateTime.tryParse` for sort keys. The FRB wrapper uses `#[frb(sync, type_64bit_int)]` so Dart gets a 64-bit `int` instead of `BigInt`, matching the `is_at_bottom` precedent.
- **Mobile sort parity beats Dart's unstable sort**: `mobile_api::sort_peers` mirrors `sort_peers_table`'s column mapping and ties on `peer_id`, so Dart replacing its `List.sort` closure (unstable on ties) with the Rust call is strictly deterministic. Descending reverses the *whole* `Ordering` (ties swapped too) on both sides, so TUI and Flutter sort identically.
- **`get_known_peers` (api wrapper) is a type anchor, not a live call path**: FRB only generates a type into the Dart surface if some non-`#[frb(ignore)]` function references it. `MobilePeerRecord` (defined in `mobile_node`, which is frb-ignored) is only reachable through the `api::get_known_peers` wrapper — so even though Flutter's peer table now prefers `getPeersWithStats`, the wrapper must stay or `MobilePeerRecord` vanishes from the generated Dart and `flutter analyze` breaks (undefined class in PeerInfoScreen/`_refreshPeers`). Keep it with a doc note pointing to the bulk call.
- **`apps/flutter_app/lib/src/rust/messages.dart` is a hand-maintained orphan, not FRB-generated output**: `PeerMessageStats` lives in `src/messages.rs`, which is NOT in FRB's `rust_input` (`crate::api,crate::mobile_api,crate::mobile_node`), so FRB never (re)generates the `PeerMessageStats` Dart class — `src/frb_generated.rs` has NO `PeerMessageStats`, and regen leaves `messages.dart` untouched. After the broadcast-stats rework the file had to be **hand-edited** to flip `broadcastReceived` → `broadcastSentToPeer` (the 2.13.0 regen didn't do it, in contrast to `mobile_api.dart`/`frb_generated.dart`/`frb_generated.rs`, which ARE regenerated). Worse, `messages.dart` was actually regenerated in an earlier session only because `PeerMessageStats` was once FRB-reachable — its classification flips between "generated" and "orphan", so it must be verified by hand after any regen/refactor that touches the stats surface. This is the same orphan class as the now-deleted `types.dart`.
- **`broadcast_recipients` is the single per-peer "broadcast" source, feeding both frontends' peer tables**: a broadcast *we* send is recorded once per connected peer (`record_broadcast_recipients` at send time, `confirmed_at` back-filled on ack), so the "Broadcast" column uniformly means *broadcasts-sent-to-peer*. This supersedes the earlier `broadcast_received` (inbound-from-peer) read in both the TUI and Flutter, and the old dormant `peers.broadcasts_sent` aggregate + its `record_broadcasts_sent` write path were removed via the `2026-09-02-000001_drop_broadcasts_sent` migration.
- **Migration version keys are the date+time, NOT the directory stem**: Diesel strips the `_name` suffix from the migration dir to derive the version key, so two migrations sharing a timestamp/`000000` collide and one silently never runs. Split related migrations with distinct suffixes (`...000000_...` / `...000001_...`). Diagnosed via a temp-DB probe of `sqlite_master` + `__diesel_schema_migrations` under `unit_messages.rs`.

## Next Steps (backlog recap — full text archived)

- Tracked as a dated backlog doc: **`docs/2026-09-02_backlog.md`**. Items 1, 2, 3, 4, 5, 8 are done (committed). **4** (the dormant `broadcasts_sent` decision) was resolved by the `broadcast_recipients` rework — the column went away entirely. **6** closed (tracking-only; expected `cargo outdated` rows documented with their upstream unblockers). **7** done: the CI dependency job is now gated on `cargo audit` (hard fail) with results uploaded as a workflow artifact (no longer committed to `main`); `cargo outdated` stays informational; the `libsqlite3-sys`/diesel `sqlite-bundled` restructure was **rejected** (Android requires a bundled SQLite — the Rust side talks FFI to `libsqlite3-sys`, not Android's platform sqlite — and the current `sqlite_bundled` feature already scopes bundling to mobile correctly). None are urgent; revisit item 6 when libp2p 0.57+ lands (its master unblocker).

## Critical Context (descriptive — archived)

- `chat_area_height` = `f.area().height - 10` (1 tab + 5 input + 1 shortcut + 1 status = 8, minus 2 border). `terminal_width` = `f.area().width`. Page size for peer paging = `chat_area_height - 2` (header + hint rows are outside the clickable area).
- Peers-table geometry after the "Peers: N" bar removal: content block starts at global row 1, so the table **header is global row 2** and **data rows start at row 3** (`handle_peer_row_click` absolute index = `start + row - 3`); message rows start at global row 2 (`first_content_row = 2`).
- **Display-name cache**: `DISPLAY_NAME_CACHE` (`src/nickname.rs`) caches the full `get_peer_display_name` string per peer id; invalidated by `impl_set_peer_field!` setters, `peers::save_peer`, and `db::set_db_url`/`reset_db_url` (`clear_display_names()` in all four cfg variants keep every `with_test_db` test cache-clean). The render loop warms all peer names on the first `TabContent::Peers` frame (`AppState.peer_names_warmed`).
- **Shared history constants**: `MAX_MESSAGE_HISTORY`/`MAX_DM_HISTORY` live in `src/types.rs`; the TUI binary re-exports them (`src/bin/tui/state.rs` → `pub use p2p_app::types::…`). Unread broadcast banner uses `AppState/TuiRenderState.chat_unread_count` (incremented in `apply_broadcast_to_state` only when `!chat_auto_scroll`; reset on scroll-to-bottom/tab-back/Esc).
- `render_frame` match (`src/bin/tui/render_loop/mod.rs`) has a `TabContent::PeerInfo(peer_id)` arm calling `render_peer_info_content`; its draw closure also performs the lazy name-cache warm-up for the Peers tab.
- `handle_mouse_left_click(state, mouse_row, mouse_column, is_peers_tab)` keeps its 4-arg signature (tests depend on it); message clicks are gated on the active tab being Chat/Log/Direct.
- `calc_visible_strings(&VecDeque<String>, auto_scroll, scroll_offset, text_width, usable_height) -> (usize, usize)` and `row_to_visible_index(&[usize], first_content_row, click_row) -> Option<usize>` are the helpers used for hit-testing.
- **FRB regeneration protocol**: `flutter_rust_bridge_codegen generate` (installed 2.13.0, pinned `=2.13.0` in Cargo.toml and `2.13.0` in pubspec) → re-append `clippy::all, clippy::pedantic, clippy::nursery, clippy::restriction` to `src/frb_generated.rs`'s `#![allow(...)]` → `cargo fmt --all` → delete untracked `src/frb_generated.h` → verify `cargo ct` + `cargo test --all-features` + `flutter analyze`/`flutter test`. `pub mod generated;` in `src/lib.rs` carries the `/// flutter_rust_bridge:ignore` doc line that makes FRB skip the diesel schema (tables incl. the relocated `peer_name_history` and the new `broadcast_recipients` all live in `src/generated/schema.rs`; `src/nickname.rs` imports it via `use crate::generated::schema::peer_name_history;`). `build.rs` regenerates `src/generated/columns.rs` (`SCHEMA_ENTRIES`) from `schema.rs` at build time, so `peer_name_history` columns are now ensured on old DBs too.
- **Dependency audit config**: `.cargo/audit.toml` ignores `RUSTSEC-2026-0118`/`RUSTSEC-2026-0119` (hickory-proto 0.25.2 — unfixable while libp2p stays 0.56, verified latest). Since the Dioxus desktop feature was removed, the audit output is down to warnings-only from core libp2p: unmaintained `paste` 1.0.15 (RUSTSEC-2024-0436, a build-time proc-macro macro crate pulled in by `if-watch`/netlink via libp2p-mdns — benign)`. The GTK3 family and rand-0.7.3 advisories that came through dioxus/wry are gone. `cargo-audit` 0.22.2 installed locally for future checks. The CI dependency job gates on `cargo audit` (hard fail) and uploads results as an artifact (`.github/workflows/dependencies.yml`); `cargo outdated` is informational with its expected rows documented in backlog item 6 (master unblocker: libp2p 0.57+).
- **Canonical peer-sort path (both frontends)**: `fmt::parse_last_seen_ms` is the one parser (space/T, `%Y-%m-%dT%H:%M:%S`, 0 on failure); the TUI sorts via `tui_helpers::sort_peers_table` and the mobile/Dart side sorts via `mobile_api::sort_peers` over `PeerSortInput`. `type_64bit_int` makes Dart's `parseLastSeenMs` return `int` (not `BigInt`).